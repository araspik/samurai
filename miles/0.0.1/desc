```
rule "foo" {
	cmd "gcc foo.c -o foo"
	in "foo.c"
	out "foo"
}
```

$ smake
foo: "foo.c" -> "foo" via "gcc foo.c -o foo"

SMake can read rules, and format and output them.
  Rules can be named.
  Single commands are accepted.
  Inputs and outputs are accepted.
